---
 - hosts: localhost
   connection: local
   gather_facts: no
   sudo: yes
   tasks:
       - name: Create a new file
       - file:
           path: /sa-user.sql
           state: touch
       - name: Insert/update Ansible powershell file 
         blockinfile: 
           path: /sa-user.sql
           


